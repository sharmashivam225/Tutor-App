<h1>Tutor-App</h1>
 <%= link_to t('.subjects', :default => t("helpers.links.Add_Subjects")),
              subjects_path, :class => 'btn btn-default'  %>

 <%= link_to t('.subjects', :default => t("helpers.links.Add_categories")),
              subject_categories_path, :class => 'btn btn-default'  %>

 <%= link_to t('.subjects', :default => t("helpers.links.SignUp")),
              new_teacher_session_path, :class => 'btn btn-default'  %>